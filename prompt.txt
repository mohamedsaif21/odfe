ODFE — PHASE 4 — SUPABASE DATABASE ALIGNMENT
MODULE: Dashboard Analytics SQL ONLY

We are working on the existing ODFE Cafe POS SaaS project.

IMPORTANT:
Do NOT modify the frontend.
Do NOT modify TypeScript.
Do NOT modify inventory services.
Do NOT modify migrations unrelated to dashboard analytics.
Do NOT rename database columns.
Do NOT create duplicate tables.
Do NOT guess the schema.

We are fixing ONLY:

supabase/dashboard_analytics.sql

CURRENT SUPABASE ERROR:

ERROR: 42703: column "is_active" does not exist

KNOWN LIVE DATABASE FACTS:

The live `inventory_items` table has:

* id UUID
* cafe_id UUID
* name
* unit
* current_stock
* minimum_stock
* cost_per_unit
* is_active
* created_by
* created_at
* updated_at

Therefore, for inventory_items the canonical live columns are:

stock            → current_stock
reorder_level    → minimum_stock
cost_price       → cost_per_unit

IMPORTANT:
The repository must NOT assume the old inventory column names.

Previously verified dashboard inventory logic contains:

* is_active
* current_stock
* minimum_stock
* cost_per_unit

but the current SQL still produces an `is_active does not exist` error somewhere in the complete dashboard analytics migration.

TASK 1 — AUDIT THE ENTIRE dashboard_analytics.sql

Read the complete file.

Find EVERY occurrence of:

is_active
stock
reorder_level
cost_price
current_stock
minimum_stock
cost_per_unit

For every occurrence, identify which table it belongs to.

Do not assume that every table has an `is_active` column.

TASK 2 — VERIFY AGAINST THE LIVE SCHEMA

Before changing anything, inspect the repository migrations/types to determine the intended schema for every table referenced by dashboard_analytics.sql.

Pay special attention to:

inventory_items
suppliers
purchase_orders
purchase_order_items
customers
expenses
expense_categories
kitchen_tickets
orders
order_items
payments
products
product_categories
cafe_tables

The live database is authoritative.

If the SQL references a column that does not exist on a table, replace it with the correct existing column only when the mapping is certain.

TASK 3 — FIX get_inventory_summary

The inventory analytics must use the LIVE inventory schema:

inventory_items.current_stock
inventory_items.minimum_stock
inventory_items.cost_per_unit
inventory_items.is_active

Correct calculations:

Total active inventory items:
COUNT(*) WHERE is_active = true

Low stock:
is_active = true
AND current_stock > 0
AND current_stock <= minimum_stock

Out of stock:
is_active = true
AND current_stock <= 0

Inventory value:

SUM(current_stock * cost_per_unit)

Only active inventory items should be included in inventory valuation.

TASK 4 — CHECK ALL OTHER `is_active` REFERENCES

If another table in dashboard_analytics.sql does NOT have an `is_active` column, do NOT blindly add one.

Instead:

1. Identify the actual live column.
2. Use the existing schema.
3. If no active/inactive concept exists for that table, remove the invalid `is_active` condition rather than inventing a column.

TASK 5 — SECURITY

Every dashboard RPC must remain:

SECURITY DEFINER
SET search_path = public

Every RPC accepting p_cafe_id must verify that the authenticated user belongs to that cafe.

Use the existing working function:

public.app_current_cafe_id()

The security condition should be equivalent to:

IF public.app_current_cafe_id() IS DISTINCT FROM p_cafe_id THEN
RAISE EXCEPTION 'Cafe access denied';
END IF;

Do NOT use:

public.current_cafe_id()

because that function does not exist in the live database.

Do NOT use auth_cafe_id() if app_current_cafe_id() is the established active-account-aware function.

TASK 6 — DO NOT CHANGE RPC RETURN TYPES

This is extremely important.

Do NOT use CREATE OR REPLACE FUNCTION if the return type differs from the existing function.

Do not change existing RPC signatures unless absolutely required.

If an existing RPC has a different return type, stop and report the mismatch instead of blindly dropping it.

TASK 7 — MAKE THE MIGRATION SAFE

The final dashboard analytics SQL must:

* Be safe to run against the CURRENT live database.
* Not rename columns.
* Not drop tables.
* Not delete data.
* Not create duplicate RPCs with conflicting signatures.
* Use CREATE OR REPLACE only when the function signature and return type are identical.
* Use IF NOT EXISTS for indexes.
* Preserve cafe isolation.

TASK 8 — CHECK ALL 10 DASHBOARD RPCs

Verify these RPCs:

1. get_dashboard_kpis
2. get_sales_trend
3. get_top_products
4. get_category_sales
5. get_customer_analytics
6. get_inventory_summary
7. get_purchase_summary
8. get_expense_summary
9. get_kitchen_summary
10. get_pos_summary

For each RPC verify:

* correct table names
* correct column names
* correct return type
* cafe isolation
* active account validation where appropriate
* no references to nonexistent columns

TASK 9 — IMPORTANT INVENTORY SCHEMA RULE

Use this mapping everywhere inside dashboard_analytics.sql:

inventory_items.stock
→ inventory_items.current_stock

inventory_items.reorder_level
→ inventory_items.minimum_stock

inventory_items.cost_price
→ inventory_items.cost_per_unit

Do NOT modify the actual database schema.

TASK 10 — BUILD / VALIDATION

After modifying the SQL:

Run:

npm run build

The build must pass.

Then report:

DASHBOARD ANALYTICS SQL FIX COMPLETE

1. Error fixed:
   [exact cause]

2. Tables audited:
   [list]

3. Column corrections:
   [list]

4. RPCs verified:
   [10 RPCs]

5. Security:
   [describe cafe ownership validation]

6. SQL file changed:
   supabase/dashboard_analytics.sql

7. Build:
   PASS / FAIL

8. Remaining database issues:
   [list or none]

IMPORTANT:
Do not claim the Supabase migration is successful merely because npm run build passes.

The SQL must still be run in Supabase SQL Editor and tested there.

If Supabase reports another `42703`, `42883`, `42P13`, or similar database error, STOP and report the exact error and the SQL section causing it instead of guessing.
