## ODFE PHASE 4 — MODULE 1 FIX

### Dashboard `recent_movements` Runtime Error

We are continuing Phase 4 Module 1 only.

The Supabase RLS/security work is already completed and the latest build passed.

Current runtime error on `/dashboard`:

```text
TypeError: inventory.recent_movements.slice is not a function

app/dashboard/page.tsx (542:51)
```

The failing code is:

```tsx
inventory.recent_movements.slice(0, 5).map(...)
```

### Goal

Fix the runtime error without changing the dashboard design, layout, database schema, RLS policies, or unrelated modules.

### Step 1 — Inspect the actual data shape

Read:

```text
app/dashboard/page.tsx
lib/services/analytics.service.ts
supabase/dashboard_analytics.sql
types/database.ts
```

Find:

1. The TypeScript type of `recent_movements`
2. The exact `get_inventory_summary` RPC return structure
3. How `analytics.service.ts` maps the RPC response
4. What the dashboard expects

Do NOT assume `recent_movements` is an array.

### Step 2 — Fix the data normalization

Make the analytics service return a predictable array for `recent_movements`.

The dashboard should always safely receive:

```ts
recent_movements: []
```

when the RPC returns `null`, an object, or no movements.

Prefer fixing the data shape in:

```text
lib/services/analytics.service.ts
```

rather than adding random defensive logic throughout the JSX.

If the RPC returns JSON/JSONB, correctly parse/normalize it before returning it to the dashboard.

Expected normalized shape:

```ts
recent_movements: Array<{
  id: string
  item_id?: string
  item_name?: string
  type: string
  quantity: number
  note?: string
  created_at: string
}>
```

Use the actual fields returned by the database if they differ. Do not invent database columns.

### Step 3 — Verify the dashboard rendering

After normalization, make sure this is safe:

```tsx
inventory.recent_movements.slice(0, 5).map(...)
```

If necessary, also make the JSX defensive:

```tsx
(Array.isArray(inventory.recent_movements)
  ? inventory.recent_movements
  : []
).slice(0, 5).map(...)
```

But only use this as a secondary safeguard. The primary fix should be in the analytics service.

### Step 4 — Check the inventory RPC

Inspect:

```sql
get_inventory_summary
```

in:

```text
supabase/dashboard_analytics.sql
```

Do NOT modify the live SQL unless the actual RPC return shape is incorrect.

The current live database has these inventory columns:

```text
inventory_items.id
inventory_items.name
inventory_items.unit
inventory_items.current_stock
inventory_items.minimum_stock
inventory_items.cost_per_unit
inventory_items.cost_price
inventory_items.stock
inventory_items.reorder_at
inventory_items.reorder_level
inventory_items.cafe_id
inventory_items.is_active
```

Stock movements use:

```text
stock_movements.id
stock_movements.inventory_item_id
stock_movements.movement_type
stock_movements.quantity
stock_movements.notes
stock_movements.created_at
stock_movements.cafe_id
stock_movements.created_by
stock_movements.is_wastage
```

Do NOT use:

```text
stock_movements.item_id
stock_movements.type
stock_movements.note
```

because the verified live schema uses:

```text
inventory_item_id
movement_type
notes
```

### Step 5 — Preserve existing functionality

Do NOT:

* redesign the dashboard
* remove dashboard sections
* change RLS
* change authentication
* rename database columns
* create new tables
* change unrelated analytics RPCs
* modify Module 2
* modify inventory business logic unless required for this specific runtime error

### Step 6 — Test

Run:

```bash
npm run build
```

Then verify `/dashboard` loads without:

```text
inventory.recent_movements.slice is not a function
```

If the build fails, fix the errors before reporting completion.

### Final response

Report:

```text
MODULE 1 DASHBOARD FIX COMPLETE

Runtime error:
  inventory.recent_movements.slice is not a function

Root cause:
  [actual cause discovered]

Files changed:
  [list]

Data normalization:
  [what was changed]

Dashboard:
  recent_movements now safely renders as an array

SQL changes:
  [none / list]

Build:
  PASS / FAIL

Remaining issues:
  [list or none]
```

Do not report completion until `npm run build` passes.
